# Copy to .env for local runs (this file is safe to commit; .env is gitignored).
# In GitHub, set GMAIL_USER and GMAIL_APP_PASSWORD as Actions *secrets*,
# and TRACK_URL as an Actions *variable*.
TRACK_URL=http://order.is/your-weekly-code
GMAIL_USER=you@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password
